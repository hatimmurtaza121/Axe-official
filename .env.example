# Server-only. Do not prefix with NEXT_PUBLIC_.
# Set the same names in Vercel → Project → Settings → Environment Variables.
CONTACT_WORKER_URL=https://axe-contact.axeofficial.workers.dev
CONTACT_WORKER_SECRET=replace-with-a-long-random-secret
